import styled from 'styled-components/native'

export const Container = styled.View`
  flex: 1;
  position: relative;
  z-index: 0;

  margin: 22px 20px;
`
export const BoxLogo = styled.View`
  margin: 40px 0 45px;
  align-items: center;
  width: fit-content;
  height: fit-content;
`
export const StateLabels = styled.View`
  flex-direction: row;
  justify-content: space-between;
  padding: 20px 0;
  margin-top: 12px;
`
